export type SiteConfig = {
    author: string;
    title: string;
    description: string;
    includeViewTransitions: boolean;
};

export type SiteMetadata = {
    title: string;
    description?: string;
}